    <button><a href="./realtimeproducts" class ="btn btn-warning">Productos en tiempo real</a></button>
    <button><a href="./carts" class ="btn btn-warning">Carritos</a></button>
    <button><a href="./users" class ="btn btn-warning">Usuarios</a></button>
    <button><a href="./login" class ="btn btn-warning">Ingresar</a></button>


    


<div class="container mt-5">
    <h1 class="text-center mb-4">PRODUCTS</h1>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3">
        {{#each listadeproductos.payload}}
        <div class="col mb-4">
            <div class="card h-100 bg-light">
                <div class="card-header bg-primary text-white">
                    <i class="bi bi-tag"></i> Code: {{this.code}}
                </div>
                <div class="card-body">
                    <h5 class="card-title text-white">{{this.title}}</h5>
                    <ul class="list-unstyled">
                        <li>
                            <img src="{{this.thumbnail}}" alt="{{this.title}}" class="img-fluid mt-2">
                        </li>
                        <li><i class="bi bi-info-circle"></i> ID: {{this.id}}</li>
                        <li><i class="bi bi-file-text"></i>{{this.description}}</li>
                        <li><i class="bi bi-currency-dollar"></i> Precio: ${{this.price}}</li>
                        <li><i class="bi bi-grid"></i> {{this.category}}</li>
                        <li><i class="bi bi-check-circle"></i> Status: {{this.status}}</li>
                        <li><i class="bi bi-box"></i> Stock: {{this.stock}}</li>
                    </ul>
                    <button class="btn btn-success add-to-cart" data-product-id="{{this.id}}">
                        Add to Cart
                    </button>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>


<!-- src/views/home.handlebars -->

<nav>
    {{#if listadeproductos.hasPrevPage}}<a href="{{listadeproductos.prevLink}}">Anterior</a>{{/if}}
    <span>Página {{listadeproductos.page}} de {{listadeproductos.totalPages}}</span>
    {{#if listadeproductos.hasNextPage}}<a href="{{listadeproductos.nextLink}}">Siguiente</a>{{/if}}
</nav>

<script src="/socket.io/socket.io.js"></script>

<script>



</script>